#include <stdio.h>

int main()
{
	//string practice
	//declare sting in varible
	//strings are declared as character arrays
	char myStr[] = "Hello World this is my first string in C";

	/*printing strings uses format specifier %s
	and no brackets when the variable is called */
	printf("%s\n", myStr);

	return 0;
}
